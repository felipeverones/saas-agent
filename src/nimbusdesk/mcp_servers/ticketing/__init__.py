"""ticketing-mcp-server — CRUD over fake support tickets, exposed via MCP.

Runs as its own process: `python -m nimbusdesk.mcp_servers.ticketing`.
Write operations (create/update ticket) are the ones that will demonstrate the
consent/human-in-the-loop flow in phase 7.
"""
